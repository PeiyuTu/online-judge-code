#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
	long long a,b;
	while(cin>>a>>b){
		long long gcd=__gcd(a,b);
		//cout<<gcd;
		cout<<(a/gcd)*(b/gcd)*gcd<<endl;
	}
	return 0;
}
